# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*~
.vscode/
.idea/

# Local scan output (never commit a real machine's scan)
disk-health.json

# Node (only if tooling is ever added)
node_modules/
npm-debug.log*

# Cloudflare / Wrangler local cache (per-machine; not for git)
.wrangler/
